"use client";

import CustomerProfile from "@/app/(main)/customer/profile/CustomerProfile";

/** Seller personal profile (addresses, WhatsApp, avatar). Store details live at /seller/store-details. */
export default function VendorProfile() {
  return <CustomerProfile />;
}
